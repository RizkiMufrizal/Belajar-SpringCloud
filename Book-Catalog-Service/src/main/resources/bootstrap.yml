spring:
  application:
    name: book-catalog
  cloud:
    config:
      fail-fast: true
      discovery:
        enabled: true
        service-id: configuration-server
eureka:
  client:
    service-url:
      defaultZone: http://localhost:10001/eureka/,http://localhost:10002/eureka/